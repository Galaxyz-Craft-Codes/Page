const url = window.location
const refs = new URLSearchParams(url.search)

var blasiverefs = ["Ppauloa37", "Starter", "PBP", "Pombo"]

function brefs() {
    for (var b = 0; b < blasiverefs.length; b += 1) {
        if (refs.get("ref") === blasiverefs[b]) {

        }    
    }
}

function bh() {
    
}

brefs()